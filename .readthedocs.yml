version: 2

python:
  version: 3.7
  install:
    - requirements: docs/requirements.txt
    - requirements: requirements.txt
    - method: pip
      path: .
      extra_requirements:
        - docs
  system_packages: true

